import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App />, //Nessa parte esta rederiçando o arquivo app.js que o contem os arquivos para mostrar no browser
  document.getElementById('root')
);
